// We export the root of our project as a module so that we can properly
// resolve the path of the project regardless of where our /build directory ends up
// eslint-disable-next-line
module.exports.root_directory = __dirname;
